<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add Voucher | Admin</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles/admin.css">
</head>
<body class="admin-body">
  <%- include('admin-navbar') %>

  <div class="admin-container" style="max-width: 760px;">
    <div class="admin-title">Add Voucher</div>
    <div class="admin-subtitle">Create a new discount promotion.</div>

    <div class="admin-card mt-3 admin-form">
      <form method="POST" action="/admin/vouchers/add">
        <div class="mb-3">
          <label class="form-label">Start Date (Publish At)</label>
          <input type="datetime-local" name="publish_at" class="form-control" required>
        </div>
        <div class="mb-3">
          <label class="form-label">End Date (Expire At)</label>
          <input type="datetime-local" name="expire_at" class="form-control" required>
        </div>
        <div class="mb-3">
          <label class="form-label">Voucher Code</label>
          <input type="text" name="code" class="form-control" required>
        </div>
        <div class="mb-3">
          <label class="form-label">Type</label>
          <select name="type" class="form-select" required>
            <option value="percent">Percentage</option>
            <option value="fixed">Fixed Amount</option>
          </select>
        </div>
        <div class="mb-3">
          <label class="form-label">Amount</label>
          <input type="number" name="amount" step="0.01" class="form-control" required>
        </div>
        <div class="mb-3">
          <label class="form-label">Minimum Spend ($)</label>
          <input type="number" name="minSpend" step="0.01" class="form-control">
        </div>
        <div class="d-flex gap-2">
          <button class="admin-btn admin-btn-primary">Create Voucher</button>
          <a href="/admin/vouchers" class="admin-btn admin-btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
