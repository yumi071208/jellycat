<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>JellyMart</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles/theme.css">
</head>

<body class="storefront">
  <%- include('navbar') %>

  <div class="container my-5">
    <div class="store-shell">
      <section class="hero-slider mb-4">
        <div class="hero-track">
          <div class="hero-slide">
            <div>
              <h2>Everyday Essentials, Thoughtfully Curated</h2>
              <p>Fresh groceries. Honest pricing. Limited stock.</p>
              <a href="/shopping" class="pastel-btn">Shop Now</a>
            </div>
            <img src="/images/bread.png" alt="Fresh bread">
          </div>
          <div class="hero-slide slide-two">
            <div>
              <h2>Seasonal Picks for the Week</h2>
              <p>Top fruits and chilled goods delivered daily.</p>
              <a href="/shopping?category=Fruits" class="pastel-btn">Browse Fruits</a>
            </div>
            <img src="/images/apples.png" alt="Seasonal fruits">
          </div>
          <div class="hero-slide slide-three">
            <div>
              <h2>Home Care, On Repeat</h2>
              <p>Household staples ready to restock.</p>
              <a href="/shopping?category=Household" class="pastel-btn">Shop Household</a>
            </div>
            <img src="/images/bleach.png" alt="Household essentials">
          </div>
        </div>
      </section>

      <section class="mb-4">
        <div class="chip-row">
          <div class="chip-card">
            <img src="/images/apples.png" alt="Fruits">
            <div>
              <strong>Fruits & Vegetables</strong>
              <div class="text-muted small">Daily freshness</div>
            </div>
          </div>
          <div class="chip-card">
            <img src="/images/salmon.png" alt="Seafood">
            <div>
              <strong>Meat & Seafood</strong>
              <div class="text-muted small">Chilled selection</div>
            </div>
          </div>
          <div class="chip-card">
            <img src="/images/milk.png" alt="Dairy">
            <div>
              <strong>Dairy & Chilled</strong>
              <div class="text-muted small">Keep it cool</div>
            </div>
          </div>
          <div class="chip-card">
            <img src="/images/cheetos.png" alt="Snacks">
            <div>
              <strong>Snacks</strong>
              <div class="text-muted small">Crunch ready</div>
            </div>
          </div>
          <div class="chip-card">
            <img src="/images/detergent.png" alt="Household">
            <div>
              <strong>Household</strong>
              <div class="text-muted small">Stocked up</div>
            </div>
          </div>
          <div class="chip-card">
            <img src="/images/greentea.png" alt="Beverages">
            <div>
              <strong>Beverages</strong>
              <div class="text-muted small">Hydration</div>
            </div>
          </div>
        </div>
      </section>

      <section class="layout-card mb-4">
        <div class="section-title mb-3">
          <span>Shop the Store</span>
          <a href="/shopping">View all</a>
        </div>

        <form class="row g-3 align-items-end" method="GET" action="/shopping">
          <div class="col-md-3">
            <label class="small text-muted">Sort</label>
            <select class="form-select" name="sort">
              <option value="">Default</option>
              <option value="popular">Most Popular</option>
              <option value="rating">Best Rating</option>
              <option value="lowhigh">Price: Low - High</option>
              <option value="highlow">Price: High - Low</option>
            </select>
          </div>
          <div class="col-md-5">
            <label class="small text-muted">Search</label>
            <input type="text" class="form-control" name="search" placeholder="Search groceries...">
          </div>
          <div class="col-md-3">
            <label class="small text-muted">Category</label>
            <select class="form-select" name="category">
              <option value="">All Categories</option>
              <option value="Snacks">Snacks</option>
              <option value="Beverages">Beverages</option>
              <option value="Vegetables">Vegetables</option>
              <option value="Fruits">Fruits</option>
              <option value="Household">Household</option>
              <option value="Meats">Meats</option>
              <option value="Alcohol">Alcohol</option>
            </select>
          </div>
          <div class="col-md-1 d-grid">
            <button type="submit" class="pastel-btn">Apply</button>
          </div>
        </form>
      </section>

      <section class="layout-card">
        <div class="section-title mb-3">
          <span>Featured Products</span>
          <a href="/shopping">View all</a>
        </div>

        <% const hasProducts = products && products.length > 0; %>

        <% if (!hasProducts) { %>
          <p class="text-muted">No products available right now.</p>
        <% } else { %>
          <div class="row g-4">
            <% for (let i = 0; i < Math.min(products.length, 8); i++) { %>
              <% const item = products[i]; %>
              <div class="col-6 col-md-3">
                <div class="product-card h-100 d-flex flex-column">
                  <a href="/product/<%= item.id %>">
                    <img src="/images/<%= item.image %>" class="jc-product-img" alt="<%= item.productName %>">
                  </a>
                  <div class="mt-2">
                    <a href="/product/<%= item.id %>" class="product-name d-block"><%= item.productName %></a>
                  </div>
                  <div class="mt-1 price-main">$<%= Number(item.price).toFixed(2) %></div>
                  <div class="mt-auto pt-2">
                    <% if (item.quantity <= 0) { %>
                      <button class="pastel-secondary-btn w-100" disabled>Unavailable</button>
                    <% } else { %>
                      <form action="/add-to-cart/<%= item.id %>" method="POST" class="d-flex gap-2">
                        <input type="number" name="quantity" value="1" min="1" max="<%= item.quantity %>" class="jc-qty">
                        <button type="submit" class="pastel-btn w-100">Add</button>
                      </form>
                    <% } %>
                  </div>
                </div>
              </div>
            <% } %>
          </div>
        <% } %>
      </section>
    </div>
  </div>
</body>
</html>
