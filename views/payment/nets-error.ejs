<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>NETS Error</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles/theme.css">
</head>
<body>
  <%- include("../navbar") %>

  <div class="container my-5" style="max-width: 720px;">
    <div class="card shadow-sm p-4">
      <h2 class="text-center mb-2">NETS Payment Error</h2>
      <p class="text-center text-muted">We couldn't create a NETS QR payment.</p>

      <div class="alert alert-danger mt-3" role="alert">
        <strong>Error:</strong> <%= error || "Unknown error" %>
      </div>

      <div class="mt-3">
        <p class="mb-1"><strong>Order ID:</strong> <%= orderId || "N/A" %></p>
        <p class="mb-1"><strong>Amount:</strong> $<%= Number(amount || 0).toFixed(2) %></p>
        <% if (statusCode) { %>
          <p class="mb-1"><strong>Status Code:</strong> <%= statusCode %></p>
        <% } %>
      </div>

      <% if (responseData) { %>
        <details class="mt-3">
          <summary>Response Details</summary>
          <pre class="mt-2"><%= responseData %></pre>
        </details>
      <% } %>

      <div class="text-center mt-4">
        <a href="/checkout" class="btn btn-outline-secondary">Back to Checkout</a>
      </div>
    </div>
  </div>
</body>
</html>
