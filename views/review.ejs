<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Write a Review | JellyMart</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles/theme.css">
</head>

<body>
  <%- include('navbar') %>

  <div class="container my-5" style="max-width: 650px;">
    <div class="layout-card">
      <h3 class="text-center mb-3">Review for <span class="text-primary"><%= product.productName %></span></h3>

      <form method="POST" action="/review/<%= product.id %>">
        <label class="form-label">Rating</label>
        <select name="rating" class="form-select" required>
          <option value="5">5 - Excellent</option>
          <option value="4">4 - Good</option>
          <option value="3">3 - Average</option>
          <option value="2">2 - Poor</option>
          <option value="1">1 - Very Bad</option>
        </select>

        <label class="form-label mt-3">Comment</label>
        <textarea name="comment" class="form-control" rows="5" placeholder="Share your experience..." required></textarea>

        <div class="d-flex justify-content-between mt-4">
          <a href="/product/<%= product.id %>" class="pastel-secondary-btn">Cancel</a>
          <button class="pastel-btn">Submit Review</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
